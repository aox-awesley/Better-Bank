# Better Bank

A RuneLite plugin that gives the OSRS bank an automatic category system. Items
are classified into sensible categories out of the box — no manual tagging — and
the bank view can be grouped by those categories.

Better Bank is a rendering and bookkeeping layer only: it never modifies the
real bank on the server.
